% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datagen.R
\name{datagen}
\alias{datagen}
\title{Generate aggregate study data from (possibly different) pharmacometric models}
\usage{
datagen(studies, model = NULL, control = datagenControl())
}
\arguments{
\item{studies}{A named list of study specifications.  Each element is a list
with:
\describe{
\item{\code{model}}{An nlmixr2-style model function with \code{ini()} and
\code{model()} blocks.  Serves as the data-generating model for this
study.  May differ between studies.  Can be omitted if a top-level
default is supplied via the \code{model} argument.}
\item{\code{times}}{Numeric vector of observation times.}
\item{\code{ev}}{A dosing event table created with \code{rxode2::et()}.}
\item{\code{n}}{(Optional) integer sample size; stored as metadata and
used when supplying the result to \code{admControl()}.}
\item{\code{model_cov}}{(Optional) the source model's OWN parameter
covariance, as that model's analysis reported it. Supplying it is what
makes a standard error meaningful for a study generated from a
published model: such a study is not a sample, its \code{E}/\code{V} are exact
functions of the source's parameters, so the only uncertainty in the
chain is the source's own and the fit propagates it by the delta
method. Without it \code{n} is read as a sample size instead, and the
reported SE then falls as \code{1/sqrt(n)} --- a factor you choose by
typing a number, not a property of the evidence.
A \strong{named} matrix, whose dimnames are the source model's \code{ini()}
parameter names. The names are load-bearing twice over: they say which
parameter each row is, and they fix the SCALE, which is the scale that
model's \code{ini()} uses --- log for a theta written \code{tcl <- log(5)}, an SD
for a residual, a variance for an omega. That is also the scale
nlmixr2 prints estimates on.
It must cover every parameter the source ESTIMATES. A parameter left
out contributes no uncertainty at all, which asserts the source knew it
exactly and reports an SE that is too small, so an incomplete matrix
warns and no standard error is reported. A \code{fix()}ed parameter is an
assertion and must NOT appear. A diagonal --- a paper's relative
standard errors, with no correlations --- is a valid fallback: it is
exact at the source's own covariate reference and degrades only as you
extrapolate away from it.}
\item{\code{cov_dist}}{(Optional) the covariate distribution this study's
subjects span --- see \code{\link[=covDraw]{covDraw()}} for the grammar. The generated
\code{E}/\code{V} are MARGINAL over it, which is what a publication reports.
Needs \code{datagenControl(method = "mc")} or \code{"gh"}; \code{"fo"} integrates
over the random effects only and is refused. Prefer \code{"gh"}, which
adds no Monte Carlo noise to data that is meant to BE the reference.}
\item{\code{stratify}}{(Optional) \code{TRUE} to stratify on every covariate this
study's OWN data-generating model conditions on, marginalising the
rest --- the split is read from the model, so it cannot disagree with
it. Two sources sharing one \code{cov_dist} therefore stratify differently,
each according to what it fitted. A character vector names the
covariates explicitly instead. The study is expanded into
one ordinary study per covariate stratum, named \verb{<study>_s1},
\verb{<study>_s2}, ..., each pinned at its own covariate value, carrying its
own effective size \code{n_k} (the quadrature weight times \code{n}, summing to
\code{n}), and marginalising the remaining covariates over their
distribution CONDITIONAL on that stratum. Use it when the source
reports --- or, being a published model, can report --- summaries by
covariate subgroup. Stratify only on what the source actually fitted:
a source with no term in a covariate has no contrast in it to give, and
nodes that vary it manufacture a null one. See \code{\link[=covStrata]{covStrata()}}.}
\item{\code{strata_nodes}}{(Optional) strata per stratified covariate
(default 5); a discrete covariate is cut at its levels instead. Each
stratum costs a solve and more of them do not buy accuracy: a matched
one-covariate fit recovers 0.7000 / 0.7002 / 0.7005 at 3 / 4 / 10
strata against a true 0.700. See \code{\link[=covStrata]{covStrata()}}.}
\item{\code{observations}}{(Optional) a named list to generate data for several
observed outputs (multi-compartment). Each entry gives one output's
\code{output} (model prediction variable, e.g. \code{"cp"}), \code{times}, and
optionally \code{ev}/\code{n} (inherited from the study otherwise). When present,
the study result carries a matching \code{observations} list of per-output
\code{E}/\code{V}, ready to pass straight to \code{admControl(studies = ...)}.}
}}

\item{model}{Optional default model function used for any study that does not
supply its own \code{model} element.  At least one of \code{model} or each
study's \code{model} must be non-\code{NULL}.}

\item{control}{A \code{\link[=datagenControl]{datagenControl()}} object.}
}
\value{
A named list with one element per study.  Each element contains:
\describe{
\item{\code{E}}{Population mean vector at \code{times}.}
\item{\code{V}}{Population covariance matrix
(\code{length(times)} x \code{length(times)}; ML denominator \code{n_sim} for
\code{method = "mc"}, the analytical FO covariance for \code{method = "fo"},
or the GH weighted covariance for \code{method = "gh"}).
The diagonal carries the model's residual-error variance; to generate
residual-free (IIV-only) moments, omit the error term from the model.}
\item{\code{n}}{Sample size (\code{NA_integer_} if not supplied).}
\item{\code{times}}{Observation times.}
\item{\code{ev}}{Dosing event table.}
\item{\code{samples}}{Raw \verb{n_sim x length(times)} prediction matrix
(only when \code{control$return_samples = TRUE}).}
}
}
\description{
Generates population mean vectors (\code{E}) and covariance matrices
(\code{V}) for each study by integrating over the IIV distribution -- either by
Monte Carlo (the default) or by a deterministic First-Order expansion
(\code{method = "fo"}, see \code{\link[=datagenControl]{datagenControl()}}).  Each study may specify its own PK/PD model (as would be the
case when digitising data from several published studies, each fit with a
different structural model).  True parameter values are taken from the
\code{ini()} block of each study's model.  Each element of the returned list
is ready to supply directly to \code{admControl(studies = ...)}.
}
\details{
With \code{control = datagenControl(method = "mc")} (the default) population
moments are computed via the same Monte Carlo engine as \code{est = "admc"}:
\deqn{E_t = \bar{f}_s(\hat\theta_s, \eta_i, t)}
\deqn{V_{ts} = \widehat{\mathrm{Cov}}_\eta[f_{s,t}, f_{s,s'}] + \Sigma_s}
where \eqn{f_s} and \eqn{\hat\theta_s} are the model and initial estimates
from the \code{ini()} block of study \eqn{s}, the sample covariance uses the
ML denominator \code{n_sim}, and \eqn{\Sigma_s} is diagonal with entries
determined by that study model's residual error type (additive, proportional,
or log-normal).

With \code{method = "fo"} the moments are instead the deterministic First-Order
expansion used by \code{est = "adfo"}:
\deqn{E = f_s(\hat\theta_s, 0)}
\deqn{V = J \Omega_s J^\top + \Sigma_s, \quad J_{tj} = \partial f_{s,t}/\partial \eta_j |_{\eta = 0}}
with the Jacobian \eqn{J} obtained from the sensitivity model (or finite
differences if that is unavailable). This is the natural choice for design
evaluation and optimal design: the moments are fast and reproducible, and
because the data-generating and data-analytic models coincide, the FO Hessian
of the log-likelihood (the expected information matrix) is evaluated at the
true maximum rather than at a point that is not an MLE of the generated data.
Note \code{est = "adfo"} always adds \eqn{\Sigma} to its predicted covariance, so
for a consistent FIM keep the residual error in the generating model; omit it
only when residual-free (IIV-only) moments are genuinely what you want.

With \code{method = "gh"} the moments are computed by deterministic
Gauss-Hermite quadrature over the random-effects prior \eqn{\eta \sim N(0, \Omega)}:
\deqn{E = \sum_q w_q f(\hat\theta, \eta_q), \quad V = \sum_q w_q (f_q - E)(f_q - E)^\top + \Sigma}
where \eqn{(\eta_q, w_q)} are the Cholesky-scaled tensor-product GH nodes and
weights. Unlike FO this is unbiased at any IIV magnitude; unlike MC the result
is noise-free and exactly reproducible. Matching the moments of \code{est = "adgh"}
makes \code{method = "gh"} the natural choice for optimal design with that estimator.

Models are compiled and cached on first use (keyed by model expression
digest), so repeated calls or multiple studies sharing the same model incur
only a single compilation.
}
\examples{
\donttest{
library(rxode2)

pk_model <- function() {
  ini({
    tcl <- log(5); tv <- log(30)
    prop.sd <- c(0, 0.2)
    eta.cl ~ 0.09; eta.v ~ 0.04
  })
  model({
    cl <- exp(tcl + eta.cl)
    v  <- exp(tv  + eta.v)
    d/dt(central) <- -(cl/v) * central
    cp <- central / v
    cp ~ prop(prop.sd)
  })
}

study_data <- datagen(
  studies = list(
    study1 = list(times = c(1, 2, 4, 8, 12, 24),
                  ev = rxode2::et(amt = 100), n = 200L)
  ),
  model   = pk_model,
  control = datagenControl(n_sim = 2000L)
)

# E and V plug directly into admControl(studies = ...)
round(study_data$study1$E, 2)
}
}
\seealso{
\code{\link[=datagenControl]{datagenControl()}}, \code{\link[=admControl]{admControl()}}
}
