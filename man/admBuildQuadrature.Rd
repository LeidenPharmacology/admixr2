% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariate.R
\name{admBuildQuadrature}
\alias{admBuildQuadrature}
\title{Build quadrature nodes and weights for covariate marginalisation}
\usage{
admBuildQuadrature(
  covariate_dist,
  method = c("gl", "gh", "taylor"),
  n_nodes = 9L,
  truncation_sd = 3.5,
  h = 2,
  order = 2L
)
}
\arguments{
\item{covariate_dist}{Named list describing the covariate distribution. Each
covariate is a list with \code{mu} and \code{sd}, e.g.
\code{list(wt = list(mu = 70, sd = 10))}. Optional \code{rho} (scalar correlation
applied to all pairs) or \code{Sigma} (explicit covariance matrix) add
correlation for the Taylor method.}

\item{method}{\code{"gl"} (Gauss-Legendre over a truncated normal), \code{"gh"}
(Gauss-Hermite, exact normal) or \code{"taylor"} (Laplace, 3 or 5 nodes
per covariate depending on \code{order}, plus cross nodes when correlated).}

\item{n_nodes}{Number of nodes per covariate for \code{"gl"}/\code{"gh"}.}

\item{truncation_sd}{Half-width of the Gauss-Legendre integration interval,
in covariate SDs.}

\item{h}{Finite-difference step (in covariate units) for the \code{"taylor"}
Hessian approximation. Scalar or one value per covariate dimension. Sets
the spacing between evaluation nodes; the 2nd-order curvature is estimated
as \verb{(NLL₊ − 2·NLL₀ + NLL₋) / h²} and the 4th-order curvature as
\verb{(NLL₊₂ − 4NLL₊ + 6NLL₀ − 4NLL₋ + NLL₋₂) / h⁴}. A step of 1–5
covariate units is typically adequate. Ignored for \code{"gl"} and \code{"gh"}.}

\item{order}{Approximation order for \code{"taylor"}: \code{2} (default, 3 nodes per
covariate) or \code{4} (5 nodes per covariate — adds the
\eqn{\sigma^4\,\mathrm{NLL}^{(4)}/8} correction term). Ignored for
\code{"gl"} and \code{"gh"}.}
}
\value{
A list with the quadrature specification: \code{method}, \code{cov_names},
\code{d} (number of covariates), \code{mu}, \code{sd}, \code{Sigma_cov}, \code{wt_nodes}
(numeric vector for 1 covariate, \verb{n_node x d} matrix otherwise),
\code{weights} (\code{NULL} for Taylor), \code{h}, \code{order}, \code{node_signs} (Taylor only)
and \code{is_correlated}.
}
\description{
Computes the covariate node values and integration weights used to
marginalise an aggregate-data model over a (multivariate) normal covariate
distribution. Pass the result to \code{\link[=admBuildCovStudies]{admBuildCovStudies()}} to build a study
list, and to \code{admControl(quadrature = )} so the \code{admc} estimator combines
the per-node -2LL contributions correctly.
}
\examples{
q_gl <- admBuildQuadrature(list(wt = list(mu = 70, sd = 10)), "gl", n_nodes = 9L)
length(q_gl$wt_nodes)

}
