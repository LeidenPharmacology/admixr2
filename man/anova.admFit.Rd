% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{anova.admFit}
\alias{anova.admFit}
\title{Compare nested aggregate-data fits}
\usage{
\method{anova}{admFit}(object, ...)
}
\arguments{
\item{object}{An \code{admFit}.}

\item{...}{One or more further \code{admFit} objects to compare it with.}
}
\value{
A \code{data.frame} of class \code{anova.admFit}, one row per model ordered
smallest to largest, carrying \code{dOFV}, \code{Weight}, the corrected \code{p} and the
uncorrected \code{p.naive} beside it for contrast.
}
\description{
\code{anova()} on two or more \code{admFit} objects gives the likelihood-ratio test with
the correction misspecification requires -- no separate function to learn, the
ordinary idiom carries the corrected reference distribution.
}
\details{
The chi-squared reference distribution is a consequence of the information
equality \code{H = J}, which holds only under correct specification. The aggregate
objective is a quasi-likelihood, so \code{dOFV} is not chi-squared and testing it
against one rejects far too often -- measured at \strong{20.2\% for a nominal 5\%}.

The limit is instead a weighted sum of chi-squares, \verb{sum_i lambda_i chi2_1},
with \code{lambda} the eigenvalues of \verb{A^-1 B}, \code{A} the naive covariance of the
tested block and \code{B} its sandwich covariance. Under correct specification
every \code{lambda_i} is 1 and this reduces to the ordinary test exactly, so the
\code{Weight} column doubles as a diagnostic: values far from 1 say the uncorrected
test would have been badly wrong.

The correction is a monotone rescaling of \code{dOFV}, so at matched actual size it
is the SAME test and gives up no power. What it supplies is the threshold: the
critical value that actually delivers 5\% ranged from 4.1 to 14.0 across
configurations, and it cannot be tabulated, because it depends on the
between-subject spread and the residual model of the problem at hand. Obtained
any other way it needs a simulation under the null for every comparison.

The larger model must be fitted with \code{covMethod = "r,s"}, which is what stores
the \code{H} and \code{J} the weights are built from.

Refused rather than approximated: non-nested comparisons (that is a Vuong
test) and boundary restrictions such as \code{omega^2 = 0}, whose limit is a
chi-bar-squared mixture the expansion behind this does not cover.

One thing no rescaling repairs: a misspecified model can converge to a
non-zero pseudo-true value even where a covariate has no effect (measured
\code{+0.0117}, p < 1e-4, with a proportional residual). That cannot be detected
from a single fit.
}
\examples{
\dontrun{
f1 <- nlmixr2(mod_cov, admData(), est = "adgh",
              control = adghControl(studies = st, covMethod = "r,s"))
f0 <- nlmixr2(mod_nocov, admData(), est = "adgh",
              control = adghControl(studies = st, covMethod = "r,s"))
anova(f1, f0)
}
}
