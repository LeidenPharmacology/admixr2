% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariate.R
\name{admBuildCovStudies}
\alias{admBuildCovStudies}
\title{Build a covariate-stratified study list from quadrature nodes}
\usage{
admBuildCovStudies(agg_list, quad, ev, times, n, prefix = "study")
}
\arguments{
\item{agg_list}{List of per-node aggregate data, one element per quadrature
node (matching \code{quad$wt_nodes} order). Each element is a list with \code{E}
(mean vector) and \code{V} (covariance matrix or variance vector).}

\item{quad}{Quadrature object from \code{\link[=admBuildQuadrature]{admBuildQuadrature()}}.}

\item{ev}{\code{rxode2::et()} dosing event table (shared across nodes).}

\item{times}{Numeric vector of observation times (shared across nodes).}

\item{n}{Sample size used for -2LL scaling (per node).}

\item{prefix}{Study-name prefix; nodes are named \verb{<prefix>01}, \verb{<prefix>02}, ...}
}
\value{
A named list of normalised study specifications, each carrying \code{cov}
(covariate values at that node) and \code{weight} (quadrature weight).
}
\description{
Pairs each quadrature node (covariate value + weight) from
\code{\link[=admBuildQuadrature]{admBuildQuadrature()}} with the corresponding per-node aggregate data to
produce a \code{studies} list for \code{admControl(studies = )}.
}
