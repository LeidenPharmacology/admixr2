% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/admc.R
\name{admControl}
\alias{admControl}
\title{Control settings for the ADM estimator}
\usage{
admControl(
  studies = list(),
  n_sim = 5000L,
  sampling = c("sobol", "halton", "torus", "lhs", "rnorm"),
  algorithm = NULL,
  maxeval = 500L,
  ftol_rel = .Machine$double.eps^2,
  print = 10L,
  seed = 12345L,
  cores = rxode2::rxCores(),
  nDisplayProgress = .Machine$integer.max,
  grad = c("sens", "fd", "cfd", "none"),
  grad_h = 1e-04,
  cov_h = 0.001,
  cov_h_outer = .Machine$double.eps^(1/5),
  grad_bounds = 5,
  covMethod = c("r", "none"),
  cov_n_sim = 10000L,
  n_restarts = 1L,
  restart_sd = 0.5,
  workers = 1L,
  rxControl = NULL,
  calcTables = FALSE,
  compress = TRUE,
  ci = 0.95,
  sigdig = NULL,
  sigdigTable = NULL,
  addProp = c("combined2", "combined1"),
  optExpression = TRUE,
  sumProd = FALSE,
  literalFix = TRUE,
  returnAdmr = FALSE,
  resid_nodes = 81L,
  gill = FALSE,
  ...
)
}
\arguments{
\item{studies}{Named list of study specifications. Each element is a list with:
\itemize{
\item \code{E} -- observed mean vector
\item \code{V} -- observed covariance matrix or variance vector (auto-detected)
\item \code{n} -- sample size
\item \code{times} -- numeric vector of observation times
\item \code{ev} -- \code{rxode2::et()} dosing event table
\item \code{method} -- \code{"cov"} or \code{"var"} (optional; auto-detected from \code{V})
}

\strong{Multi-compartment (multiple observed outputs).} To fit several observed
compartments simultaneously (e.g. plasma and brain/CSF), give the study an
\code{observations} list instead of top-level \code{E}/\code{V}/\code{times}. Each entry is one
observed output with its own \code{output} (the model prediction variable, e.g.
\code{"cp"} or \code{"cCSF"}), \code{times}, \code{E}, \code{V} and -- for independent fits -- \code{ev}
and \code{n}. Pass the endpoint names to \code{\link[=admData]{admData()}}, e.g.
\code{admData(c("cp", "cCSF"))}, so nlmixr2 recognises every endpoint. There are
two modes:
\itemize{
\item \emph{Independent} -- each observed output has its own \code{n}/\code{ev} (separate
experiments / subjects, e.g. a plasma study and a brain study combined for
meta-analysis). The outputs are independent likelihood blocks and the
aggregate \verb{-2LL} is their sum.
\item \emph{Joint (same subjects)} -- the outputs are measured on the SAME subjects.
Give the study a shared \code{n} and \code{ev}, and a joint covariance either as a
study-level full matrix \code{V} (blocks in \code{observations} order) or as
per-output marginal \code{V} plus a \code{cross} list of cross-covariance blocks
keyed \code{"outA:outB"} (each \code{length(times_A)} x \code{length(times_B)}; omitted
pairs are zero). The compartments are then scored by a single MVN over the
stacked vector with shared random effects. \code{est = "adirmc"} does not
support multiple observed outputs; use \code{"admc"}, \code{"adfo"} or \code{"adgh"}.
}

\strong{Long format (one row per endpoint/time).} As an alternative to the
\code{observations} list, a study may carry a \code{data} frame that keys each observed
summary by endpoint, the way nlmixr2 keys observations by \code{DVID}/\code{CMT}. The
frame needs an endpoint column (\code{DVID}, \code{CMT} or \code{output}), a time column
(\code{TIME}), a mean column (\code{E}) and -- unless a joint \code{V} is given -- a
variance column (\code{V}) or an SD column (\code{SD}). It is normalised into exactly
the same units as the \code{observations} form, so the two are interchangeable:

\if{html}{\out{<div class="sourceCode">}}\preformatted{# independent blocks: per-row variances; optional per-endpoint `n` column
# and per-endpoint `ev` (a list of event tables keyed by endpoint)
list(n = 60L, ev = ev,
     data = data.frame(DVID = c("cp", "cp", "cCSF"), TIME = c(1, 2, 2),
                       E = c(9.1, 7.4, 2.2), V = c(1.2, 0.9, 0.1)))

# joint (same subjects): ONE stacked covariance whose rows/cols align with
# the rows of `data` -- no `cross` blocks to assemble by hand
list(n = 60L, ev = ev, data = data.frame(DVID = ..., TIME = ..., E = ...),
     V = V_joint)
}\if{html}{\out{</div>}}

A study-level \code{V} (or an explicit \code{joint = TRUE}) marks the endpoints as
same-subject; without one, each endpoint is an independent likelihood block.
Endpoints are stacked in the order they first appear in \code{data}.}

\item{n_sim}{Number of Monte Carlo samples per NLL evaluation.}

\item{sampling}{Sampling method for eta draws: \code{"sobol"} (Sobol, default),
\code{"halton"} (Halton), \code{"torus"} (Kronecker/torus), \code{"lhs"} (Latin hypercube),
or \code{"rnorm"} (iid normal).}

\item{algorithm}{nloptr algorithm string, or \code{NULL} (default) to pick the
default that matches \code{grad}: \code{"NLOPT_LD_LBFGS"} with a gradient,
\code{"NLOPT_LN_BOBYQA"} when \code{grad = "none"}. Any algorithm reported by
\code{\link[nloptr:nloptr.print.options]{nloptr::nloptr.print.options()}} is accepted (e.g. \code{"NLOPT_LD_MMA"},
\code{"NLOPT_LN_NELDERMEAD"}). An explicit algorithm is reconciled with \code{grad}:
when \code{grad = "none"} a gradient-based algorithm (\verb{NLOPT_LD_*} /
\verb{NLOPT_GD_*}) falls back to \code{"NLOPT_LN_BOBYQA"}; when a gradient is
requested a derivative-free algorithm (\verb{NLOPT_LN_*} / \verb{NLOPT_GN_*}) turns
the gradient off. Both emit a message.}

\item{maxeval}{Maximum number of optimizer function evaluations.}

\item{ftol_rel}{Relative function-value tolerance for convergence.}

\item{print}{Print progress every this many evaluations (0 = silent).}

\item{seed}{Random seed for reproducibility.}

\item{cores}{Number of OpenMP threads for \code{rxSolve()}. Defaults to
\code{rxode2::rxCores()}. \code{rxSolve()} parallelises over subjects, so this is the
main speed lever for the MC estimators; when \code{workers > 1} it is a \emph{total}
budget, split across the workers.}

\item{nDisplayProgress}{Passed to \code{rxSolve()}: the solver shows its text
progress bar only once a single solve exceeds this many subjects. The
default (\code{.Machine$integer.max}) keeps the bar off, which is what you want
for scripts, vignettes and logs; lower it (e.g. \code{1000L}) to see solver
progress during long interactive fits.}

\item{grad}{Gradient mode: \code{"sens"} (sensitivity equations, default), \code{"fd"}
(forward finite differences), \code{"cfd"} (central finite differences), or
\code{"none"} (derivative-free). A warning is issued when \code{"sens"} is requested
but the sensitivity model is unavailable; the estimator then falls back to
forward finite differences.}

\item{grad_h}{Step size for finite-difference gradient evaluation during
optimization (used by \code{grad = "fd"} or \code{"cfd"}). The default 1e-4 is near
the optimal balance between truncation error (grows with \code{h}) and MC noise
amplification (grows as \code{1/h}) for forward FD. Central FD (\code{"cfd"}) has a
slightly wider optimum around 1e-3, but 1e-4 works well for both.}

\item{cov_h}{Inner FD step for the gradient-based Hessian (only used when
\code{covMethod = "r"} and \code{grad != "none"}). Each gradient evaluation has MC
noise of order \code{sigma / cov_h}; the Hessian divides that noise by the outer
step, giving total noise \verb{sigma / (cov_h * cov_h_outer * |p|)}. \code{cov_h = 1e-3}
balances truncation error and noise amplification. Increase to \code{1e-2} if the
Hessian is non-positive definite.}

\item{cov_h_outer}{Outer step scale for the numerical Hessian. The actual step
for parameter \code{p} is \verb{max(|p|, 0.1) * cov_h_outer}. Applied to both the
gradient-FD Hessian (\code{grad != "none"}) and the NLL-FD Hessian
(\code{grad = "none"}). Default \code{eps^(1/5)} (~2.5e-3) is larger than the
textbook \code{eps^(1/4)} to account for MC noise in NLL and gradient evaluations;
empirically it matches the analytical (sensitivity-equation) Hessian ground
truth. Increase (e.g. to \code{5e-3} or \code{1e-2}) if the Hessian is non-positive
definite.}

\item{grad_bounds}{Box-constraint half-width when using gradients: the fit is
confined to \verb{p0 +/- grad_bounds} on the optimizer scale, which for a
log-scale parameter is a factor of \code{exp(grad_bounds)} (~148 at the default
5). This bound is admixr2's, not the model's -- an unbounded parameter has
no other -- and nloptr reports normal convergence at a box corner, so a
warning is emitted if an estimate finishes on it.}

\item{covMethod}{Covariance method: \code{"r"} (numerical Hessian over the
structural, residual-error and omega parameters) or \code{"none"}. Omega is
included because excluding it also biases the STRUCTURAL standard errors
downward -- a theta carrying an eta is correlated with that eta's variance.
If the weakly-identified omega Cholesky makes the Hessian non-positive
definite, the structural + residual sub-block is reported with a warning.

All three blocks are reported on the scale the ESTIMATES are printed on, as
\code{nlmixr2est} does: structural thetas on the log/optimizer scale, residual
error as an SD, and omega as the variance/covariance entries (named
\verb{om.<eta>} and \verb{cov.<eta_i>.<eta_j>}). The omega block is rotated by the
full Jacobian of Omega with respect to the log-Cholesky, which is not
diagonal once omega is correlated.}

\item{cov_n_sim}{Number of MC samples for the covariance (Hessian) step.
More samples reduce MC noise in NLL evaluations. The NLL-based Hessian
(\code{grad = "none"}) uses a central second difference of the NLL with the
same Sobol sequence (CRN) at every perturbed point, so noise largely
cancels and \code{cov_n_sim = 10000} (default) is sufficient for most models.}

\item{n_restarts}{Number of optimization restarts. Runs in parallel when
\code{workers > 1}.}

\item{restart_sd}{Standard deviation of structural theta perturbations for
restart initialisation.}

\item{workers}{Number of parallel workers for multi-restart. \code{1} (default)
runs restarts sequentially. Values \verb{> 1} run the restarts on a pool of
background R processes (mirai daemons), which behaves the same way on every
platform. Requires the \code{mirai} package. Workers are stopped automatically
after the restart phase so all cores are available for the Hessian step; if
a fit is interrupted, \code{admStopWorkers()} cleans up any survivors.}

\item{rxControl}{\code{rxode2::rxControl()} object. Created automatically when \code{NULL}.}

\item{calcTables, compress, ci, sigdigTable, optExpression, sumProd, literalFix}{Passed to \code{nlmixr2est::foceiControl()} for the table/output machinery.}

\item{sigdig}{Significant digits asked of the ODE solver, or \code{NULL} (the
default) to leave rxode2's own solver tolerances alone. When set, it is
passed to \code{rxode2::rxSolve()}'s own \code{sigdig} argument for every solve the
estimator issues -- rxode2 owns the mapping to \code{atol}/\code{rtol} and has changed
it between releases, which is why the digits, not the tolerances, are what
travels -- and to \code{nlmixr2est::foceiControl()} for the post-fit tables.

It is a speed lever, and an opt-in one because it is not free. The
estimators finite-difference the solve with steps of the same order:
\code{grad_h} (1e-4), \code{cov_h} (1e-3) and \code{cov_h_outer} (~2.5e-3), while
\code{sigdig = 4} maps to a relative tolerance of ~1e-4 on current rxode2.
Differencing a solution whose own noise is 1e-4 with a 1e-4 step returns
noise, and it surfaces as a moved objective and an indefinite covariance
Hessian (every \code{SE} reported \code{NA}) rather than as an error. Most worthwhile
where the gradient is fully analytic and nothing differences the solve --
\code{adfoControl(grad = "analytical")} measured ~4.8x faster at \code{sigdig = 4}
with standard errors unchanged to 4 significant figures. Elsewhere, compare
the objective and the standard errors against \code{NULL} before relying on it.
Table formatting is unaffected either way: \code{sigdigTable} defaults to 4
regardless.}

\item{addProp}{How combined additive+proportional error is parameterised in
the nlmixr2 output tables: \code{"combined2"} (default, variance form) or
\code{"combined1"} (SD form). Has no effect on admixr2's own estimation; passed
to \code{nlmixr2est::foceiControl()} for the table/output machinery only.}

\item{returnAdmr}{If \code{TRUE}, return a plain list instead of a full nlmixr2
fit object (useful for debugging).}

\item{resid_nodes}{Gauss-Hermite nodes used to integrate the RESIDUAL for a
transform-both-sides endpoint (\code{boxCox}, \code{yeoJohnson}, \code{logitNorm},
\code{probitNorm}), where \code{y = g(h(f) + sigma*eps)} has no closed-form mean and
variance. Ignored by every other error model, which has closed forms. Default
81. Measured worst-case relative error against an independent quadrature, over
all four transforms and residual SD of 0.5, 1, 2 and 3: n = 15 gives 5.7e-2,
31 gives 4.5e-3, 81 gives 5.0e-5. The error is dominated by large residual SD;
at SD <= 1, n = 31 already gives 1e-7 or better.

This is an ACCURACY dial, not a speed one. The quadrature is linear in
\code{resid_nodes} in isolation (~50 us at 15, 300 us at 81 for an 8-row study) but
negligible beside the ODE solve: a full NLL evaluation measured 0.750 s per 60
evaluations at BOTH 31 and 81 nodes. Raise it if you have a saturating endpoint
with a large residual SD; there is little to gain by lowering it.}

\item{gill}{Use the Gill, Murray, Saunders & Wright (1983) algorithm to choose
the finite-difference steps, instead of the fixed \code{cov_h_outer} and \code{grad_h}
scales. \code{FALSE} (the default) keeps the existing steps, so nothing moves
unless you ask.

A fixed scale is one guess about how much noise the objective carries,
applied identically to every parameter -- and it is the guess behind the
"Hessian not positive definite ... try increasing \code{cov_h_outer}" warning.
Gill83 probes the objective instead and returns, per parameter, the step at
which condition error and truncation error balance, so a parameter the
objective is flat in and one it is sharp in get different steps and the
choice tracks the ODE tolerance rather than assuming one. It is the same
algorithm FOCEI uses for its own steps (\code{nlmixr2est::nlmixr2Gill83()}), so
the two packages agree on what a finite difference is. Worth trying first
when the Hessian comes back indefinite.

Applied to the post-fit covariance Hessian and to the optimizer's gradient,
for the parameters each actually steps in PARAMETER space: for \code{admc} the
whole vector when a joint (same-subject) study has no sensitivity model, and
otherwise the structural thetas the sensitivity model has no column for; for
\code{adirmc} the whole vector under \code{grad = "fd"}. A theta with no mu-referencing
eta does NOT qualify by itself -- the sensitivity model emits a direction for
each, so its gradient is exact and no step is chosen for it.

\code{admc}'s ETA perturbations keep the fixed \code{grad_h} regardless. They have no
parameter to key on, and they difference the PREDICTION rather than the
objective, which is a different function from the one Gill83 measured; under
common random numbers the two solves share their draws, so most of the MC
noise a step choice would guard against cancels anyway.

Measured ONCE per fit -- for the gradient at the starting values, as FOCEI
does at its first gradient evaluation. \code{adirmc} measures on its first outer
iteration and reuses: its proposals are redrawn each iteration, so
re-measuring would cost ten inner evaluations per parameter every time.
Costs up to ten extra objective evaluations per finite-differenced
parameter. A parameter Gill83 reports as flat or linear falls back to the
fixed scale.

Because it reaches the gradient, \code{gill = TRUE} can change the FIT and not
only the standard errors -- but only when the gradient has a
finite-differenced parameter to begin with.}

\item{...}{Additional arguments (none allowed; triggers an error).}
}
\value{
An object of class \code{admControl}.
}
\description{
Constructs a control object for \code{est = "admc"}, the Monte Carlo aggregate
data modelling estimator.
}
\examples{
# Minimal control object -- inspect defaults
ctl <- admControl()
ctl$n_sim
ctl$algorithm

# Override key settings without fitting
ctl2 <- admControl(
  n_sim    = 2000L,
  maxeval  = 300L,
  grad     = "fd",
  seed     = 42L
)

\donttest{
library(rxode2)
library(nlmixr2)

data("examplomycin")
obs   <- examplomycin[examplomycin$EVID == 0, ]
obs   <- obs[order(obs$ID, obs$TIME), ]
times <- sort(unique(obs$TIME))
ids   <- unique(obs$ID)
dv_mat <- do.call(rbind, lapply(ids, function(i) {
  sub <- obs[obs$ID == i, ]; sub$DV[order(sub$TIME)]
}))
E <- colMeans(dv_mat)
V <- cov.wt(dv_mat, method = "ML")$cov

pk_model <- function() {
  ini({
    tcl <- log(5);  tv1 <- log(12); tv2 <- log(25)
    tq  <- log(12); tka <- log(1.2)
    prop.sd <- c(0, 0.2)
    eta.cl ~ 0.09; eta.v1 ~ 0.09; eta.v2 ~ 0.09
    eta.q  ~ 0.09; eta.ka ~ 0.09
  })
  model({
    cl <- exp(tcl + eta.cl); v1 <- exp(tv1 + eta.v1)
    v2 <- exp(tv2 + eta.v2); q  <- exp(tq  + eta.q)
    ka <- exp(tka + eta.ka)
    d/dt(depot)      <- -ka * depot
    d/dt(central)    <- ka * depot - (cl/v1 + q/v1) * central + (q/v2) * peripheral
    d/dt(peripheral) <- (q/v1) * central - (q/v2) * peripheral
    cp <- central / v1
    cp ~ prop(prop.sd)
  })
}

fit <- nlmixr2(
  pk_model, admData(), est = "admc",
  control = admControl(
    studies  = list(study1 = list(E = E, V = V, n = length(ids),
                                  times = times, ev = et(amt = 100))),
    n_sim    = 1000L,
    maxeval  = 200L
  )
)
print(fit)
}

}
