% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/study-api.R
\name{admStudy}
\alias{admStudy}
\title{Write down a published study}
\usage{
admStudy(
  model = NULL,
  est = NULL,
  E = NULL,
  V = NULL,
  sd = NULL,
  sem = NULL,
  n = NULL,
  times = NULL,
  dose = NULL,
  ev = NULL,
  population = NULL,
  at = NULL,
  by = NULL,
  strata_nodes = NULL,
  range = NULL,
  label = NULL,
  v_denom = NULL
)
}
\arguments{
\item{model}{The published model, as an nlmixr2-style function (or a parsed
\code{rxUi}). Omit for a digitised study.}

\item{est}{Named vector of the paper's parameter estimates, on the scale the
model's \code{ini()} is written on --- so \code{tcl = log(5.2)} for a clearance
reported as 5.2. Omitted parameters keep the model's own \code{ini()} value.}

\item{E, V, sd, sem}{Digitised aggregate data: the reported mean profile \code{E},
with its spread as \code{sd} (per timepoint), \code{sem} (converted using \code{n}), or a
full covariance \code{V}.}

\item{n}{Number of subjects the study reports on. Taken from \code{population}
when that is the cohort's data frame.}

\item{times}{Observation times.}

\item{dose}{Dose amount, as shorthand for a single-dose \code{ev}.}

\item{ev}{A dosing event table from \code{\link[rxode2:et]{rxode2::et()}}, for anything \code{dose}
cannot express.}

\item{population}{The population the study enrolled --- see
\code{\link[=admPopulation]{admPopulation()}}. Covariates the model reads are integrated over it.
A \strong{data frame} of enrolled subjects is accepted directly and read into
a table, taking \code{n} from its row count. EVERY column that is not a
reserved data column (\code{ID}, \code{TIME}, \code{DV}, \code{AMT}, ...) becomes a covariate,
including ones this model does not read --- a covariate another source
fitted is still evidence here, and each one adds a quadrature axis, so
drop the columns you do not want integrated over.}

\item{at}{Named list pinning a covariate at a value, for a study reported in
one subgroup, e.g. \code{at = list(SEX = 1)}. A pinned covariate must be omitted
from \code{population}.}

\item{by}{Covariate the paper reports results SEPARATELY by, e.g.
\code{by = "SEX"}. Expands a published model into one study per level; digitised
subgroup profiles must be supplied as separate studies.}

\item{strata_nodes}{Quadrature resolution for a banded CONTINUOUS
covariate, i.e. how many nodes it is cut into. Default 9. Raise it for
precision, at a multiplicative cost: a source banded on two continuous
covariates becomes \code{strata_nodes^2} studies, and each is solved. Discrete
covariates are unaffected --- their levels are exact and cost one stratum
each. Note that the objective VALUE depends on this, so two fits are only
comparable at the same resolution; admixr2 records it per study and
\code{\link[=anova]{anova()}} checks it.}

\item{range}{Optional named list giving the covariate span the source
ENROLLED, e.g. \code{range = list(WT = c(52, 118))}. The declared distribution
is truncated to it, whether that covariate ends up conditional (strata are
then cut from the truncated margin) or marginal (the truncated margin is
integrated over). Needed when that distribution is
wider than the enrolment --- a \verb{mean +/- SD} transcribed from a baseline
table has tails where nobody was --- and \emph{not} wanted when \code{population} is
the patients themselves, since the margins are then fitted to exactly who
was enrolled and truncating would score the paper against a sub-population
of its own. An unnamed value is allowed only when one covariate is banded.}

\item{label}{Optional display name; otherwise taken from the argument name in
\code{\link[=admStudies]{admStudies()}}.}

\item{v_denom}{Which denominator the supplied spread uses: \code{"unbiased"}
(\code{n - 1}) or \code{"ml"} (\code{n}). Usually leave it unset --- the currency you
wrote the study in already says which it is, and \code{admStudy()} resolves it
and shows the answer when the study is printed.

\code{sd}/\code{sem} default to \code{"unbiased"}, because a \strong{published} spread is the
\code{n - 1} one; \code{V} defaults to \code{"ml"}, because handing over a covariance
matrix is the deliberate act of someone who computed it, and the
likelihood wants the ML denominator. Set it explicitly when your source
breaks that pattern --- a \code{sd} you computed yourself with the ML
denominator, say. A published \code{model} is always \code{"ml"}: its moments are
generated at that denominator, so \code{"unbiased"} is refused rather than
applied to a \code{V} that is already right.}
}
\value{
An \code{admStudy} object.
}
\description{
One study, transcribed from one paper. It holds what the paper reported and
nothing else, is checked as you build it, and prints so the transcription can
be read back against the source before anything is fitted.
}
\details{
A study contributes in one of two currencies, and this takes either:
\itemize{
\item \strong{a published model} --- \code{model}, with the paper's parameter table as
\code{est};
\item \strong{digitised aggregate data} --- \code{E} with \code{sd} (or \code{sem}, or \code{V}).
}

A study generated from a model is not a sample: its mean and covariance are
exact functions of that model's parameters, and \code{n} sets its RELATIVE WEIGHT
against the other studies rather than its precision. No standard error is
reported for a fit that includes one --- see \code{\link[=admStudies]{admStudies()}}.

Nothing is solved here. The study is generated when it reaches the fit, so
building one is cheap and a mistake surfaces on \code{print()} rather than after a
long run.
}
\section{Conditional and marginal covariates are derived, not declared}{


Whether a covariate is \strong{conditional} or \strong{marginal} for a source is a
property of that source's own model, so admixr2 works it out and there is
nothing to set.

A covariate is \strong{conditional} when the source's model ESTIMATED its
coefficient. That paper reports a contrast along it, so the source is banded
into strata and the contrast is carried into the fit. A covariate the model
merely READS is not banded --- weight at a fixed allometric exponent carries
no fitted effect to recover, and banding on it would buy strata and no
evidence.

A covariate is \strong{marginal} when the model did not estimate it. There is no
contrast in that paper to band on, and splitting it on a covariate its model
never saw would manufacture evidence, so admixr2 integrates over the
population instead. That covers both the covariate the model never mentions
and the one it reads at an asserted coefficient; \code{\link[=print]{print()}} names them
separately, because only the second is easy to mistake for conditional.

The band set comes from the source and from nothing else --- in particular
not from the model being fitted. A covariate the ANALYSIS model never reads
is still banded on, and each stratum then has that margin dropped from its
design, which is exact. Resolving it against the analysis model instead would
make a nested pair band differently: the null model drops the covariate, and
two fits over different study sets have no likelihood ratio between them.

This matters and is why it is not left to the caller: a covariate every
source marginalises is not identified against a random effect on the same
parameter. Measured over 720 replicates, coverage was 0.933 with one source
banded and 0.925 with all of them, against \strong{0.817 with none}.

A source given as digitised \code{E}/\code{V} has no model to derive from and is
marginal over whatever it declares. Use one \code{admStudy(..., at = ...)} per
reported subgroup to enter a paper that published by subgroup.

\code{stratify} was removed; \code{\link[=covStrata]{covStrata()}} still takes it for working with a
covariate distribution directly. \code{strata_nodes} and \code{range} remain: the
first is a precision setting and the second is a fact about the source,
neither of which is a statement about which covariates are conditional.
}

\seealso{
\code{\link[=admStudies]{admStudies()}} to collect several, \code{\link[=admPopulation]{admPopulation()}} for the
baseline table.
}
