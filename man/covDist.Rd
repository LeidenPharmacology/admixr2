% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariate.R
\name{covDist}
\alias{covDist}
\title{Describe the covariate distribution a study's subjects span}
\usage{
covDist(..., cor = NULL, joint = NULL, dist = c("normal", "lnorm"))
}
\arguments{
\item{...}{One argument per covariate, named as the model reads it. Each is
one of:
\describe{
\item{\code{c(mean = , sd = )}}{Mean and SD on the covariate's own scale ---
what a baseline table reports. Normal by default; \code{dist = "lnorm"}
moment-matches a lognormal margin instead, which is what you want for
a covariate that must stay positive (see \code{dist}).}
\item{\code{c(mu = , sd = )} / \code{c(meanlog = , sdlog = )}}{A normal or
lognormal margin given directly on its own scale.}
\item{\code{c(label = prob, ...)}}{A categorical covariate: names are the
level labels, values their proportions. Levels are coded \verb{0, 1, ...}
in the order given.}
\item{a \code{list(...)}}{The canonical form, for anything else --- including
\code{list(quantile = f)} for an arbitrary margin.}
}
Alternatively a single data.frame with \code{covariate}, \code{mean} and \code{sd}
columns (and optionally \code{dist}): a baseline-characteristics table
transcribed as-is.}

\item{cor}{Correlation between the covariates: a scalar for two of them, or
a correlation matrix (named, in any order). Realised through a Gaussian
copula on the declared margins.}

\item{joint}{An arbitrary sampler, for dependence a correlation cannot
express. It receives the matrix of uniforms admixr2 supplies and returns
one named column per covariate --- the shape a copula or an R-vine
produces. Overrides \code{cor}.}

\item{dist}{Default margin for the \code{c(mean = , sd = )} form: \code{"normal"}
(default) or \code{"lnorm"}. A per-covariate \code{dist} wins over it.

Choose \code{"lnorm"} when the model needs the covariate to stay positive. A
normal margin is unbounded below and the quadrature reaches 3.75 standard
deviations, so any covariate with a coefficient of variation above about
0.19 --- which includes most of them, weight at 72 +/- 16 being 0.22 ---
gets a node at or below zero, where an allometric or log term is \code{NaN}.
\code{covDist()} warns when that would happen.}
}
\value{
A validated \code{cov_dist}, ready to pass to a study. Printing it shows
each covariate's realised mean, SD and type.
}
\description{
Builds the \code{cov_dist} a study carries, from what a publication actually
reports: a mean and a standard deviation per covariate, and optionally a
correlation between them. Validation happens here, where an error can name
the covariate, rather than at the first objective evaluation of a fit.
}
\examples{
# what a baseline-characteristics table reports
cd <- covDist(WT = c(mean = 72, sd = 16), CRCL = c(mean = 90, sd = 25),
              cor = 0.6)
cd

# a categorical covariate: labels are the levels, values their proportions
covDist(SEX = c(female = 0.55, male = 0.45))

# or transcribe the table itself
covDist(data.frame(covariate = c("WT", "CRCL"),
                   mean = c(72, 90), sd = c(16, 25)))

}
\seealso{
\code{\link[=covDraw]{covDraw()}} to see what a specification describes,
\code{\link[=covStrata]{covStrata()}} to cut it into strata.
}
